import { dev } from '$app/environment';

export const title: string = 'My first blog with SvelteKit';
export const description: string = 'A simple blog made quickly using SvelteKit and open-props';
export const url: any = dev ? 'https://localhost:5173' : 'https://myblog.com';
